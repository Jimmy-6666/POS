# แผนโครงการ POS ร้านมินิมาร์ทไทย

> เอกสารนี้เป็นแผนและสถาปัตยกรรมฐานเดิม ไม่ใช่รายการงานค้างปัจจุบัน
> ใช้ `PROJECT_MAP.md` สำหรับโครงสร้างปัจจุบัน, `FEATURE_STATUS.md` สำหรับ
> สถานะล่าสุด และ `DECISIONS.md` สำหรับการตัดสินใจที่ยังมีผล

## เป้าหมายและแนวทาง

เว็บแอป Flask + SQLite สำหรับใช้งานภายในร้านบน Windows เครื่องเดียวและเปิดผ่าน LAN ได้ ไม่พึ่งอินเทอร์เน็ต, Node.js, Docker หรือ CDN โค้ดแบ่งตามคุณลักษณะเพื่อให้ดูแลและย้ายเครื่องได้ง่าย

## โครงสร้างโครงการ

```text
thai-minimart-pos/
├── app.py                 จุดเริ่มโปรแกรม
├── start-pos.bat          ตัวเปิดระบบบน Windows
├── requirements.txt
├── pos_app/
│   ├── database.py        การเชื่อมต่อ/อัปเกรดฐานข้อมูล
│   ├── schema.sql
│   ├── routes/            route แยกตามคุณลักษณะ
│   ├── services/          ตรรกะธุรกิจและ transaction (ระยะถัดไป)
│   ├── templates/         หน้า HTML ภาษาไทย
│   └── static/            CSS, JavaScript และรูปที่มากับระบบ
├── data/pos.db
├── uploads/products/
├── backups/
└── tests/
```

## หน้าและเส้นทางที่วางแผน

| หน้า | เส้นทาง | สิทธิ์ |
|---|---|---|
| เข้าสู่ระบบ | `/login` | ทุกคน |
| ภาพรวม | `/dashboard` | ผู้ดูแล/ผู้จัดการ |
| ขายสินค้า | `/pos` | ทุกบทบาท |
| สินค้า/เพิ่ม/แก้ไข | `/products`, `/products/new`, `/products/<id>/edit` | ผู้ดูแล/ผู้จัดการ |
| รับสินค้า/ปรับสต็อก/ประวัติ | `/inventory/receive`, `/inventory/adjust`, `/inventory/movements` | ผู้ดูแล/ผู้จัดการ |
| นับสต็อก | `/stock-counts` | ผู้ดูแล/ผู้จัดการ; ผู้ร่วมที่ได้รับมอบหมาย |
| ประวัติการขาย/ใบเสร็จ | `/sales`, `/sales/<id>/receipt` | ตามบทบาท |
| ปิดยอด | `/reconciliations` | เจ้าของรายการ/ผู้จัดการ/ผู้ดูแล |
| รายงาน | `/reports` | ผู้ดูแล/ผู้จัดการ |
| พนักงาน/ตั้งค่า/สำรอง | `/staff`, `/settings`, `/backups` | ผู้ดูแล |
| สถานะระบบ | `/health` | ภายใน LAN |

API จะใช้ JSON เฉพาะงานโต้ตอบ เช่น ค้นสินค้า ตะกร้า การชำระเงิน และนับสต็อก ส่วนฟอร์มทั่วไปใช้ POST + redirect เพื่อลดความซับซ้อน

## สิทธิ์ตามบทบาท

| ความสามารถ | ผู้ดูแลระบบ | ผู้จัดการ | แคชเชียร์ |
|---|:---:|:---:|:---:|
| POS | ✓ | ✓ | ✓ |
| จัดการสินค้า/สต็อก/นับสต็อก | ✓ | ✓ | — |
| รายงานและปิดยอดทั้งหมด | ✓ | ✓ | เฉพาะตนเอง |
| อนุมัติยกเลิก/คืน | ✓ | ✓ | — |
| พนักงาน/ตั้งค่า/Audit/Backup | ✓ | — | — |

ตรวจสิทธิ์บนเซิร์ฟเวอร์ทุก route; การซ่อนเมนูเป็นเพียง UX ไม่ใช่กลไกความปลอดภัย

## ลำดับการทำงานสำคัญ

### การขาย

1. เซิร์ฟเวอร์โหลดสินค้า ราคา ต้นทุน และสต็อกปัจจุบันใหม่
2. ตรวจจำนวน ส่วนลด วิธีชำระ และสิทธิ์; ไม่เชื่อยอดรวมจากเบราว์เซอร์
3. เปิด `BEGIN IMMEDIATE` และสร้างเลขใบเสร็จที่ไม่ซ้ำ
4. บันทึก sale, sale items และ payment
5. ลดสต็อกและเพิ่ม stock movement ทุกรายการ
6. commit ทั้งชุด; หากขั้นใดผิดพลาด rollback ทั้งชุด

### การเคลื่อนไหวสต็อก

การรับสินค้า การขาย การคืน การปรับ และการปิดรอบนับสต็อกต้องเรียก service กลางที่ล็อกแถวเชิงตรรกะ อ่านยอดเดิม บันทึก `previous/changed/new` แล้วจึงอัปเดตสินค้าใน transaction เดียว การรับสินค้าใช้ต้นทุนเฉลี่ยถ่วงน้ำหนัก `(ยอดเดิม×ทุนเดิม + รับใหม่×ทุนใหม่) ÷ จำนวนใหม่` และรายการขายเก็บต้นทุน ณ เวลาขาย

### การปิดยอด

`เงินสดที่ควรมี = เงินสดตั้งต้น + ขายเงินสด - คืนเงินสด - เงินนำออก + เงินเติม` แล้วเปรียบเทียบกับยอดนับจริง เก็บผลต่างพร้อมสถานะ ตรง/เกิน/ขาด วิธีสแกนและโอนแสดงแยกกัน

## สำรองข้อมูล

ระบบจะใช้ SQLite online backup API สร้างสำเนาที่สอดคล้อง แล้วรวม `pos.db` และ `uploads/products` เป็น ZIP ชื่อมีวันเวลาใน `backups/` ผู้ดูแลดาวน์โหลดได้ การคืนข้อมูลทำแบบ manual ขณะปิดเซิร์ฟเวอร์เพื่อลดความเสี่ยง

## ใช้งานผ่าน LAN

Waitress ฟังที่ `0.0.0.0:8000`; เครื่องหลักเปิด `http://127.0.0.1:8000` อุปกรณ์ร่วม Wi‑Fi เปิด `http://<IPv4-เครื่องหลัก>:8000` ระบบมีไว้สำหรับ LAN และไม่ควรเปิดพอร์ตสู่สาธารณะ

## ระยะดำเนินงาน

ใช้ 10 ระยะตาม brief: พื้นฐาน, ยืนยันตัวตน, สินค้า, POS เงินสด, วิธีจ่าย/ใบเสร็จ, สต็อก, นับสต็อก, ปิดยอด, รายงาน, audit/backup/security แต่ละระยะต้องรัน tests, ทดสอบ input ผิด, database write และ regression ก่อนเปลี่ยนสถานะเป็นเสร็จ
