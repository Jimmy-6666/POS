(() => {
  const cart = (() => { try { return JSON.parse(localStorage.getItem("onlineCartV1") || "[]"); } catch { return []; } })();
  const $ = selector => document.querySelector(selector), location = $("#deliveryLocation"), payment = $("#paymentMethod"), submit = $("#submitOrder");
  const popup = text => { $("#checkoutMessage").textContent = text; $("#checkoutMessage").classList.add("show"); setTimeout(() => $("#checkoutMessage").classList.remove("show"), 3500); };
  const money = value => `${(value / 100).toFixed(2)} บาท`;
  let subtotal = 0, valid = false;
  const key = sessionStorage.getItem("onlineOrderKey") || (crypto.randomUUID?.() || `${Date.now()}-${Math.random()}`); sessionStorage.setItem("onlineOrderKey", key);
  const fee = () => subtotal >= 10000 ? 0 : Number(location.selectedOptions[0]?.dataset.fee || 0);
  const refresh = () => { $("#deliveryFee").textContent = money(fee()); $("#grandTotal").textContent = money(subtotal + fee()); $("#roomReference").required = location.selectedOptions[0]?.dataset.roomRequired === "1"; $("#cashExpectedLabel").hidden = payment.value !== "cash"; };
  const fieldsValid = () => {
    const checks = [["#contactName", "กรุณาระบุชื่อสำหรับติดต่อ"], ["#contactPhone", "กรุณาระบุเบอร์มือถือ"], ["#deliveryLocation", "กรุณาเลือกสถานที่จัดส่ง"]];
    if ($("#roomReference").required) checks.push(["#roomReference", "กรุณาระบุห้องหรือจุดรับ"]);
    for (const [selector, message] of checks) { const field = $(selector); if (!field.value.trim()) { field.focus(); popup(message); return false; } } return true;
  };
  const summary = () => { const option = location.selectedOptions[0], cash = $("#cashExpected").value; $("#confirmationSummary").innerHTML = `<h2>สรุปออเดอร์</h2><div class="confirm-block"><span>ผู้ติดต่อ</span><strong>${$("#contactName").value} · ${$("#contactPhone").value}</strong></div><div class="confirm-block"><span>จัดส่ง</span><strong>${option.textContent}${$("#roomReference").value ? ` · ${$("#roomReference").value}` : ""}</strong></div><div class="confirm-block"><span>การชำระเงิน</span><strong>${payment.selectedOptions[0].textContent}</strong>${payment.value === "cash" && cash ? `<small>เตรียมเงิน ${Number(cash).toFixed(2)} บาท</small>` : ""}</div><div class="confirm-total"><span>รวมทั้งหมด</span><strong>${money(subtotal + fee())}</strong></div>`; };
  async function loadCart() { const response = await fetch("/order/api/cart/validate", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ items: cart.map(x => ({ product_id: x.product_id, quantity: x.quantity })) }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); subtotal = data.subtotal_satang; valid = true; $("#checkoutSubtotal").textContent = money(subtotal); refresh(); }
  $("#toConfirm").addEventListener("click", () => { if (!valid || !fieldsValid()) return; summary(); $("#deliveryStep").hidden = true; $("#confirmStep").hidden = false; $("#stepEyebrow").textContent = "ขั้นตอน 2 จาก 2"; $("#stepTitle").textContent = "ตรวจสอบและยืนยันออเดอร์"; });
  $("#editOrder").addEventListener("click", () => { $("#confirmStep").hidden = true; $("#deliveryStep").hidden = false; $("#stepEyebrow").textContent = "ขั้นตอน 1 จาก 2"; $("#stepTitle").textContent = "ข้อมูลจัดส่งและการชำระเงิน"; });
  location.addEventListener("change", refresh); payment.addEventListener("change", refresh);
  $("#savedDeliveryButton").addEventListener("click", async () => { try { const rows = await (await fetch("/order/api/delivery-history")).json(); $("#savedDeliveryList").innerHTML = rows.length ? rows.map((row, i) => `<button class="saved-delivery" type="button" data-i="${i}"><strong>${row.contact_name}</strong><span>${row.contact_phone} · ${row.location_name_snapshot} ${row.room_reference || ""}</span></button>`).join("") : "<p>ยังไม่มีข้อมูลจัดส่งเดิม</p>"; $("#savedDeliveryDialog").showModal(); $("#savedDeliveryList").onclick = event => { const button = event.target.closest("button[data-i]"); if (!button) return; const row = rows[button.dataset.i]; $("#contactName").value = row.contact_name; $("#contactPhone").value = row.contact_phone; location.value = row.delivery_location_id; $("#roomReference").value = row.room_reference || ""; refresh(); $("#savedDeliveryDialog").close(); }; } catch { popup("ไม่สามารถโหลดข้อมูลจัดส่งเดิมได้"); } });
  $("#closeSavedDelivery").addEventListener("click", () => $("#savedDeliveryDialog").close());
  submit.addEventListener("click", async () => { if (!valid || !fieldsValid()) return; submit.disabled = true; const payload = { items: cart.map(x => ({ product_id: x.product_id, quantity: x.quantity })), contact_name: $("#contactName").value, contact_phone: $("#contactPhone").value, delivery_location_id: Number(location.value), room_reference: $("#roomReference").value, payment_method: payment.value, customer_note: $("#customerNote").value, cash_expected_satang: payment.value === "cash" ? Math.round(Number($("#cashExpected").value || 0) * 100) : 0, idempotency_key: key }; try { const response = await fetch("/order/api/orders", { method: "POST", headers: { "Content-Type": "application/json", "X-CSRF-Token": $("#customerCsrf").value }, body: JSON.stringify(payload) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); localStorage.removeItem("onlineCartV1"); sessionStorage.removeItem("onlineOrderKey"); window.location.href = data.detail_url; } catch (error) { popup(error.message); submit.disabled = false; } });
  loadCart().catch(error => { popup(error.message || "ไม่สามารถตรวจสอบตะกร้าได้"); submit.disabled = true; });
})();
